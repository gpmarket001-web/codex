# Dados reais de runtime (gerados pelo Comando Manha) — nao versionar receita real
data/utmify-campaigns-latest.json
data/utmify-raw.json

# Mineracao: candidatos reais coletados + saida derivada (a fila .md fica versionada)
data/mineracao/candidatos-eua.json
data/mineracao/rank-latest.json
mineracao/_cron.log

# Diversificacao: saida derivada (o board pipeline/validacao.md fica versionado)
data/pipeline-latest.json

# Saidas derivadas dos motores (regeneradas a cada run)
data/margem-real-latest.json
data/protecao-pixel-latest.json
briefings/_cron.log

# Mantidos versionados: data/cogs-ledger.json, data/utmify-campaigns-sample.json (sintetico),
# data/toques-log.json

# Gate Revisao Bruta: checklists e relatorios por pagina (template fica versionado)
data/gate/*.json
!data/gate/_template.json
gate/*.md

# Sentinela Reportana: export real de fluxos + saida derivada (dados de recuperacao ficam versionados como amostra)
data/reportana-fluxos.json
data/reportana-sentinela-latest.json

# Criativos: midia gerada (Higgsfield) e pesada/binaria — versiona so brief/roteiro/copy
criativos/**/*.png
criativos/**/*.jpg
criativos/**/*.jpeg
criativos/**/*.webp
criativos/**/*.mp4
criativos/**/*.mov

# Meta: saida derivada
data/meta-ativacao-latest.json
